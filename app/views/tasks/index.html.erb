<h2>My tasks</h2>

<ol>
  <% @tasks.each do |task| %>
    <%= link_to task_path(task) do %>
      <li><%= task.completed %> <%= task.title %></li>
    <% end %>
  <% end %>
</ol>

<%= link_to "Add new task", new_path %>
