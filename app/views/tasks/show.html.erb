<h2><%= @task.title %></h2>
<p><%= @task.completed %></p>
<p><%= @task.details %></p>

<%= link_to "Back to tasks", index_path %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%= link_to "Edit", task_edit_path %>
